<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .square.highlight { background-color: #ffdd57 !important; }
    .piece:hover { transform: scale(1.2); transition: transform 0.1s ease; }
    .game-over-message { animation: fadeIn 0.3s ease-in-out; text-align: center; padding: 10px; }
    .piece.dragging { opacity: 0.5; }
    .chessboard {
      display: grid;
      grid-template-columns: repeat(8, minmax(0, 1fr));
      grid-template-rows: repeat(8, minmax(0, 1fr));
      width: 400px; height: 400px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
      border-radius: 10px;
    }
    .square { display: flex; align-items: center; justify-content: center; }
    .square.light { background-color: #f0d9b5; }
    .square.dark { background-color: #b58863; }
    .piece { font-size: 36px; cursor: pointer; }
    .flipped .piece { transform: rotate(180deg); }
  </style>
</head>
<body class="min-h-screen flex flex-col items-center justify-center bg-gradient-to-br from-[#1e293b] via-[#0f172a] to-[#020617] text-white">

  <h1 class="text-2xl font-bold mb-4">Multiplayer Chess</h1>
  <div class="flex gap-6">
    <!-- Chessboard -->
    <div class="chessboard w-96 h-96"></div>

    <!-- Chat box -->
    <div class="w-64 bg-zinc-800 rounded-lg shadow p-3 flex flex-col">
      <h2 class="text-lg font-semibold mb-2">Chat</h2>
      <div id="chatBox" class="flex-1 overflow-y-auto border rounded p-2 bg-zinc-900"></div>
      <div class="mt-2 flex">
        <input id="chatInput" type="text" class="flex-1 p-2 rounded-l bg-zinc-700 text-white" placeholder="Type message...">
        <button id="sendBtn" class="bg-blue-600 px-3 rounded-r">Send</button>
      </div>
    </div>
  </div>

  <script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"></script>
  <script src="/js/chessgame.js"></script>
</body>
</html>
